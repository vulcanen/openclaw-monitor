# Default install registry: company proxy of npmjs upstream.
# This is for `npm install` only — `npm publish` uses publishConfig.registry
# in package.json, which targets the public npmjs.
registry=https://registry.npmmirror.com/

save-exact=false
package-lock=true
