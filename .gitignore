node_modules/
dist/
*.log
.DS_Store
.vscode/
.idea/
coverage/
*.tsbuildinfo
.env
.env.local

# Claude Code session artifacts (per-developer) — but keep .claude/settings.json
# in the repo as a team-level config (currently: includeCoAuthoredBy=false).
.claude/*
!.claude/settings.json
